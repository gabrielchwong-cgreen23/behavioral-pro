{
  "name": "behavioral-pro",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "start": "node app.js",
    "shopify": "shopify",
    "build": "echo \"no build step\"",
    "dev": "shopify app dev",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy",
    "test": "node --test tests/**/*.test.js",
    "stress:test": "node --test tests/stress.test.js"
 
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.0",
    "axios": "^1.16.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.1",
    "express": "^5.2.1"
  },
  "trustedDependencies": [
    "@shopify/plugin-cloudflare"
  ],
  "author": "gabrielwong",
  "private": true,
  "workspaces": [
    "extensions/*",
    "packages/*"
  ]
}
